# .claude/skills/

Placeholder for project-scoped Claude Code skills. Each skill lives in its own
subfolder as `<name>/SKILL.md`. Candidates once the Pluto arms exist: running the
viewer, generating a sample `.bin`, validating an import pipeline's output.
