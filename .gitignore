node_modules/
*.bin
!lib/**
.DS_Store
Thumbs.db
.claude/settings.local.json
.obsidian/workspace*.json
.obsidian/cache

# Obsidian config lives ONLY at the repo root (root = vault)
**/.obsidian/
!/.obsidian/
/.obsidian/*.json
# graph settings follow the clone (colour groups + the filter hiding handoffs; vault/_meta/graph-colors.json is the source)
!/.obsidian/graph.json

.pdf

# production-side settings (copy of duct-config.example.psd1)
scripts/duct/duct-config.psd1
