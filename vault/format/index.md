# Format

- [[vault/format/v4-schema|v4-schema]] — block directory + element domains (current target)
- [[vault/format/v3-schema|v3-schema]] — legacy fixed-header format (frozen; loads via shim)
- [[vault/format/features-sidecar|features-sidecar]] — viewer-authored predicates / section cuts / supports JSON
